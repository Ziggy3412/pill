{
  "name": "pillpal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names web,backend,mobile --prefix-colors cyan,green,magenta \"npm run dev:web\" \"npm run dev:backend\" \"npm run dev:mobile\"",
    "dev:web": "npm run dev --prefix web",
    "dev:backend": "npm run dev --prefix backend",
    "dev:mobile": "npm run dev --prefix mobile",
    "install:all": "npm install --prefix web && npm install --prefix backend && npm install --prefix mobile",
    "build:web": "npm run build --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
